import { Box, Button } from '@mui/material';
import React from 'react';

const ButtonGroup = () => {
    return (
        <Box>
            <Button>-</Button>
            <Button>1</Button>
            <Button>+</Button>
        </Box>
    );
};

export default ButtonGroup;